export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface rsModal {
  active: boolean;
  active_two: boolean;
  step: number;
  contract_id: number;
  user_id: number;
  document1: any[];
  document2: any[];
  file_type: string;
}
